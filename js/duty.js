import {esc,first,todayName,normalize} from './utils.js';import {openModal} from './modal.js';
export function todayDuty(cms){const day=todayName();return (cms.dutyRoster||[]).find(x=>normalize(first(x,['Día','Dia']))===normalize(day))||{} }
export function dutyCard(cms){const d=todayDuty(cms);const icon=first(d,['Icono'])||'✅';const est=first(d,['Estación','Estacion'])||first(d,['Estación Principal'])||'Duty';return `<article class="card" data-open="duty"><span class="icon">${esc(icon)}</span><h3>${esc(est)}</h3><p>${esc(first(d,['Enfoque','Enfoque Principal'])||'Duty Roster de hoy')}</p><span class="badge">${todayName()}</span></article>`}
export function showDuty(cms){const day=todayName();const roster=todayDuty(cms);const details=(cms.dutyDetail||[]).filter(x=>normalize(first(x,['Día','Dia']))===normalize(day));const groups={};details.forEach(x=>{const est=first(x,['Estación','Estacion'])||'General';const cat=first(x,['Categoría','Categoria'])||'General';groups[est]??={};groups[est][cat]??=[];groups[est][cat].push(x)});let body=Object.entries(groups).map(([est,cats])=>`<div class="duty-section"><h3>${esc(est)}</h3>${Object.entries(cats).map(([cat,items])=>`<div class="duty-section"><h3>${esc(cat)}</h3>${items.sort((a,b)=>(+first(a,['Orden'])||0)-(+first(b,['Orden'])||0)).map(i=>`<div class="duty-check ${String(first(i,['Crítico','Critico'])).toLowerCase()==='true'?'critical':''}"><span class="box"></span><span>${esc(first(i,['Icono']))} ${esc(first(i,['Actividad']))}</span></div>`).join('')}</div>`).join('')}</div>`).join('')||'<p>No hay detalle para hoy.</p>';openModal(`<div class="duty-head"><span class="big">${esc(first(roster,['Icono'])||'✅')}</span><div><span class="eyebrow">Duty Roster</span><h2>${esc(day)} · ${esc(first(roster,['Estación','Estacion'])||'')}</h2><p>${esc(first(roster,['Enfoque','Enfoque Principal'])||'')}</p></div></div>${body}`)}
